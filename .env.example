# URL del backend. En native (Expo Go / build) es obligatoria. En web se
# puede dejar vacía si el sitio se sirve desde el mismo origen que la API
# (el despliegue combinado en Render).
EXPO_PUBLIC_API_URL=https://epa-ujap.onrender.com
